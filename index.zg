start{
    x = (1,2)
    y = (3,4)
    z = (5,6)
    w = (7,8)
    p = poly(x, y, z)
    printVar
    
}

method : returnPointValues(multiplier: number): point{
    return : point(multiplier * 2, multiplier * 3)
}

method : multiplyBySeventeen(input: number): number{
    return : input * 17
}

method : addNumbers(a: number, b: number): number{
    return : a + b
}